{
    /* For tests that require a new database */
    "NewDb": {
      //Database
      "DatabaseName": "FOR_NewDB",
      
      //Timeout in seconds for tests that create a new database
      "Timeout": "120" // In seconds
  
      // Needed for projects that use filestream
      //"FileStreamLocation" :  "C:/Filestream"
    }
}
